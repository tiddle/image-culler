# Phase 1 (exposure + blur) runtime dependencies.
numpy>=1.26
opencv-python>=4.9      # luminance histogram/clipping + Laplacian blur variance
rawpy>=0.19            # RAW decode via libraw (embedded preview, full fallback)

# Phase 2 (blink / closed-eye) will add:
#   mediapipe          # face mesh / eye-aspect-ratio
